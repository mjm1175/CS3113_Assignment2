public static class PublicVars
{
    /* for single door
    //public static bool hasKey = false;
    */

    // for multiple doors; size needs to be number of keys per scene
    // or = new bool[4];
    public static bool[] hasKey = {false, false, false, false};
}
